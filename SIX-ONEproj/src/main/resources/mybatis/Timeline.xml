<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매퍼파일의 완전한 경로 : xml생략 -->
<mapper namespace="mybatis.Timeline">


<insert id="timeline" parameterType="java.util.Map">
INSERT INTO timeline VALUES (SEQ_TIMELINE.nextval,#{inscontent},#{image},sysdate,#{id})
</insert>
	
<select id="timelinecontent" parameterType="java.util.Map" resultType="TimelineDTO">
SELECT t.timeline_no,t.content, t.image_name, t.created_at, t.id,m.name,m.profile 
FROM timeline t join member m on t.id=m.id
</select>
	
	
	
	
	
</mapper>