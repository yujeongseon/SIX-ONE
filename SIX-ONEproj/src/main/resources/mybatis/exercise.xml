<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매퍼파일의 완전한 경로 : xml생략 -->
<mapper namespace="mybatis.exercise">
	
	<select id="exerciseSelectList" resultType="exerciseDTO">
		select * from exercise order by exercise_name
	</select>
	
	<insert id="exerciseInsert" parameterType="java.util.Map">
		insert into exercise values(SEQ_EXERCISE.NEXTVAL,#{exerciseName},#{exercisePartials},#{exerciseMotions})
	</insert>
	
	
	
	
	
</mapper>