<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매퍼파일의 완전한 경로 : xml생략 -->
<mapper namespace="mybatis.calendar">
	
	<select id="calendarSelectList" resultType="java.util.Map">
		select * from calendar c left join play_exe e on c.play_no = e.play_no left join routine r on c.routine_no = r.routine_no left join rou_exe re on r.routine_no = re.routine_no
	</select>
	
	
	
	
	
	
</mapper>