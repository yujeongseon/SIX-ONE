<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매퍼파일의 완전한 경로 : xml생략 -->
<mapper namespace="mybatis.Graphs">

<select id="exercisecount" parameterType="java.util.Map" resultType="GraphsDTO">
SELECT * FROM play_exe p JOIN exercise e 
ON p.exercise_no = e.exercise_no
WHERE PLAY_AT  BETWEEN (SELECT TRUNC(sysdate,'iw') dt_date FROM dual)
AND (SELECT TRUNC(sysdate, 'iw') +6 dt_date FROM dual) AND ID = #{id}
</select>

	
	
	
	
</mapper>